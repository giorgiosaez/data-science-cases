name: myenv
channels:
  - defaults
dependencies:
  - python=3.6
  - pip:
    # Required packages for AzureML execution, history, and data preparation.
    - sklearn
    - azureml-sdk
    - azureml-core